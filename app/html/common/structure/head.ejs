<%
	const Sitemap = require('sitemap');
	let page = htmlWebpackPlugin.options && htmlWebpackPlugin.options.page;
    // get the page directly to hook it into webpack dev build process
	page = Sitemap.getPage(page);
	const allPages = Sitemap.getPages(page);

	const Locale = page.locale;
	const Strings = page.strings;
	const BaseUrl = page.baseUrl;

	const title = (page && page.title) || 'Title';
	const description = (page && page.description) || 'Description';
	const canonical = (page && page.canonical) || '/';
	const image = canonical + require('../../../assets/img/' + page.image) || '';

	const faviconIco = require('../../../assets/img/favicon/favicon.ico');
	const appleTouchIcon = require('../../../assets/img/favicon/apple-touch-icon.png');
	const favicon32 = require('../../../assets/img/favicon/favicon-32x32.png');
	const favicon16 = require('../../../assets/img/favicon/favicon-16x16.png');
	const manifest = require('../../../assets/img/favicon/site.webmanifest');
	const maskIcon = require('../../../assets/img/favicon/safari-pinned-tab.svg');
%>

<!DOCTYPE html>
<html lang="<%= Locale %>">
<head>

	<meta charset="utf-8">

	<title><%= title %></title>

	<meta http-equiv="X-UA-Compatible" content="IE=edge">
	<meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1">
	<meta name="description" content="<%= description %>">
	<meta property="og:description" content="<%= description %>">
	<meta property="og:title" content="<%= title %>" />

	<!-- Open Graph -->
	<meta property="og:type" content="website">
	<meta property="og:url" content="<%= canonical %>">
	<meta property="og:site_name" content="<%= title %>">
	<meta property="og:title" content="<%= title %>">
	<meta property="og:description" content="<%= description %>">
	<meta property="og:image:width" content="1200">
	<meta property="og:image:height" content="630">
	<meta property="og:image" content="<%= image %>">
	<meta property="og:image:alt" content="<%= description %>">

	<!--Twitter-->
	<meta name="twitter:card" content="summary">
	<meta name="twitter:image" content="<%= image %>">
	<!-- <meta name="twitter:site" content="@twitter account"> -->
	<meta name="twitter:title" content="<%= title %>">
	<meta name="twitter:description" content="<%= description %>">

	<!-- i18n alternatives for search engines -->
	<% allPages.forEach(p => { const { locale, baseUrl } = p  %>
		<link rel="alternate" hreflang="<%= locale %>" href="<%= canonical + baseUrl %>" />
	<% }) %>
	<link rel="alternate" hreflang="x-default" href="<%= canonical %>" />

	<!-- Favicon -->
	<link rel="shortcut icon" href="<%= faviconIco %>" type="image/x-icon">
	<link rel="apple-touch-icon" sizes="180x180" href="<%= appleTouchIcon %>">
	<link rel="icon" type="image/png" sizes="32x32" href="<%= favicon32 %>">
	<link rel="icon" type="image/png" sizes="16x16" href="<%= favicon16 %>">
	<link rel="manifest" href="<%= manifest %>">
	<link rel="mask-icon" href="<%= maskIcon %>" color="#da532c">
	<meta name="msapplication-TileColor" content="#da532c">
	<meta name="theme-color" content="#ffffff">

	<!-- Fonts -->
	<link href="https://fonts.googleapis.com/css?family=Open+Sans:400,700" rel="stylesheet">

	@require('../scripts/window.appReady.html')

	<%=
        // Application CSS files
        htmlWebpackPlugin.files.css.map(src =>
		`
	<link href="${src}" rel="stylesheet">`
      ).join("\n")
    %>
	<style>
		html{
			--css-variable-test: 1;
		}
		#css-variable-test{
			display: none;
			opacity: var(--css-variable-test);
		}
	</style>
</head>

<body class="locale-<%= Locale %>">
